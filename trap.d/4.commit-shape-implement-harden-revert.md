# A single 3-issue lane's commits progressively narrowed scope

**What was observed:** the lane closing #4/#5/#6 produced three commits with an unusual
shape for what started as one dispatch: implement R04/R05/R06 -> harden against real
security/robustness findings from self-review -> revert an over-fix (backslash
normalization, R06 case-folding) that itself regressed on real input. Flagged by the lane
itself in its report (`fix-4-20260919T232338Z.json`, adjacent item, `report-for-filing`,
`in_blast_radius: true`) as worth surfacing to the maintainer rather than squashed into a
single retelling.

**Where:** `scripts/swarm_doctor.py`, PR #23 (branch `fix/4`), commits `bce57db`, `d99e532`,
`96973b5`.

**What it cost:** nothing broken -- the honest record of two real review rounds, including a
revert of a fix that made things worse, rather than a squash that would have hidden the
regression-and-recovery from a later reader of `git log`.

**How it was confirmed:** read the three commits directly; the revert commit's own tests
document the reverted (worse) behavior explicitly rather than silently dropping it.

Judged non-blocking against the ranking table in `skills/manager/phases/findings.md` -- no
release-blocking defect, just a commit-shape observation. Logged here rather than filed as
its own issue. Worth a look if `fix_commit_scope.py`'s `needs-second-pass` threshold should
treat a pure revert differently from new feature surface when deciding whether a third review
round is mandatory (the lane's own compliance section flagged this exact ambiguity).
