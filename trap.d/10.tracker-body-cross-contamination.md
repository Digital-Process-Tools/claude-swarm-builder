# Issue #10's tracker body concatenates two unrelated texts

**What was observed:** issue #10's raw body appears to concatenate the genuine "CI for
claude-oss" description with boilerplate that reads like a doctor-rule-stub description not
matching #10's own scope. Flagged first by an earlier lane's comment, re-confirmed
independently by the lane that closed #20 (report `fix-10-20260919T225610Z.json`, adjacent
item, `report-for-filing`).

**Where:** the issue body itself, on GitHub (`gh-issue:10:full`).

**What it cost:** nothing yet beyond two lanes' worth of re-reading and re-confirming the same
observation, since it was never converted into an actionable board fact.

**How it was confirmed:** read directly, twice, by two independent lanes.

Also seen the same shape across #5, #11 and #12 (per the lane that closed #12, report
`fix-12-20260919T230419Z.json`): a stale "the doctor already declares this as a
`not_implemented` stub" paragraph copy-pasted across several issue bodies regardless of
whether any R01-R09 doctor rule actually corresponds to that issue's own scope.

Judged non-blocking (tracker hygiene, not a code defect or release blocker) against the
ranking table in `skills/manager/phases/findings.md`; logged here rather than filed as its own
issue. A triage sweep might want to clean the affected issue bodies directly instead of
opening a ticket about the tickets.
