- `reader/reader.html` now tries `fetch('swarm.json')` first, for the case it is published or
  served next to a sibling `swarm.json`, and falls back to the embedded payload it always carried
  when that fetch fails (opened via `file://`, or no sibling present). The JSON stays the thing
  you edit; rebuilding with `scripts/swarm_reader.py` keeps the embedded fallback current (#12).
