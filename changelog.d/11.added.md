- `scripts/swarm_compile.py` renders each agent's role summary, scripts list and incoming
  contract (brief fields, handback states aggregated from every edge into that agent) into a
  `<!-- swarm:begin -->` / `<!-- swarm:end -->` block in its `md`, creating the block at the top
  after frontmatter when absent. A second run against unchanged input is a byte-identical
  no-op; `--check` reports what would change without writing (#11).
