- `scripts/swarm_doctor.py` implements R07: two edges into the same node from different
  parents warn when their `when` clauses share a normalised token (underscores treated as
  separators, stopwords and short/purely-numeric tokens dropped), or when their `handback`
  lists are identical regardless of order. Warning level, naming both edge ids (#7).
