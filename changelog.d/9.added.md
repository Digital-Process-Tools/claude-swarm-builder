- `scripts/swarm_doctor.py` implements R09: for every agent with an `md` or a
  `context[].path`, scans those files for a `subagent_type: "<name>"` / `Agent(subagent_type`
  spawn string and requires a declared edge from that agent to the named one (a plugin
  prefix like `oss:triager` is matched against the bare catalogue name too). Error level,
  naming the agent and the missing target (#9).
