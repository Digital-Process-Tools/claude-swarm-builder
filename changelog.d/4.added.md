- `scripts/swarm_doctor.py` implements R04: on every edge, `agents[to].authority` must be a
  subset of `agents[from].authority`, comparing on the token before the first space or
  parenthesis (`merge (gate 3 ...)` -> `merge`). Finding at error level, `ref` is the edge id.
  Fires broadly on `examples/claude-oss.swarm.json` today -- most roles have authority disjoint
  from their parent's, not just the `developer`/`sub-manager` gap named in the acceptance line --
  shipped visibly rather than hidden; the maintainer decides which gaps are real (#4).
