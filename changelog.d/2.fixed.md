- Doctor rule R02 now does a real BFS from each flow's root over that flow's
  structured edges and reports any declared agent that is reachable from no
  flow root, instead of always answering `could-not-check` (#2).
  `harness_agents` are exempt from this check.
