- Doctor rule R01 now actually checks that every edge's `from` and `to` name a
  declared agent or harness_agent, instead of always answering `could-not-check`
  (#1). An edge pointing at an undeclared agent is an error-level finding naming
  the edge id; bare-string edge entries (used by the `tick` flow) are skipped
  rather than crashing the rule.
