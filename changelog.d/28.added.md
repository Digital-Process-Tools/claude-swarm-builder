- `scripts/swarm_doctor.py --json` now names which of `edge`/`agent` each finding is about,
  instead of one overloaded `ref` string. `scripts/swarm_reader.py` accepts `--findings PATH`
  (the doctor's `--json` output) and embeds it next to the swarm payload; the built page colours
  the named nodes and edges by finding level, shows the finding text on click, and lists a legend
  entry per rule that fired -- `could-not-check` rules included, never silently absent. `/oss:read`
  (`commands/read.md`) now runs the doctor first and wires the result in, so the overlay is on by
  default (#28).
