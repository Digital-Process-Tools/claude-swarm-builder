- `/swarm-builder:write <agent>` co-writes one agent's behaviour prose in conversation: reads
  only that agent's compiled block and the edges pointing at it, asks per declared handback
  state and per item of `authority`, writes exactly what the user confirmed under the
  skeleton's headings without ever touching the compiled block, then runs `swarm_doctor.py`
  (R05, R09). Refuses to generate more than one agent per run and refuses to fill a heading
  nobody has answered -- the fabricated-bodies failure `docs/prior-art.md` records (#31).
